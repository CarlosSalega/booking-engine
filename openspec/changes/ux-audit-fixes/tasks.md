# Tasks: UX/A11y Audit Fixes

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 650–900 (15 files: 2 new + 13 modified, plus ~8–10 RTL/unit test files) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 (slice 1a: AuthField + login) → PR 1b (slice 1b: register) → PR 2 (slice 2: feedback/theming) → PR 3a (slice 3a: breadcrumbs) → PR 3b (slice 3b: wizard + loading) |
| Delivery strategy | ask-on-risk |
| Chain strategy | pending |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 | Slice 1a — AuthField primitive + login form a11y (h1, form-level alert, toggle, sizing) | PR 1 (base: main) | `pnpm test src/components/auth/__tests__/login-form.test.tsx src/components/auth/__tests__/auth-field.test.tsx` | `pnpm dev` → visit `/login`, submit bad credentials, verify focus lands on alert | `src/components/auth/auth-field.tsx` + `src/components/auth/login-form.tsx` + login tests — revertible without touching register |
| 1b | Slice 1b — register form a11y on AuthField (per-field errors, h1, toggle, sizing) | PR 1b (base: PR 1 branch) | `pnpm test src/components/auth/__tests__/register-form.test.tsx` | `pnpm dev` → visit `/register`, submit short password, verify focus lands on first invalid field | `src/components/auth/register-form.tsx` + register tests only |
| 2 | Slice 2 — feedback/theming: tokens, Toaster, calendar, badges, WhatsApp | PR 2 (base: main or PR 1b branch per chain strategy) | `pnpm test src/components/ui/__tests__/calendar.test.tsx src/modules/bookings/presentation/__tests__/status-tones.test.ts` | `pnpm dev` → light + dark toggle: fire toast, open calendar range, view dashboard badges + WhatsApp CTA | `src/app/globals.css` + `src/app/layout.tsx` + `src/components/ui/calendar.tsx` + badge/tone/WhatsApp files + 2 lockstep test files |
| 3a | Slice 3a — DashboardBreadcrumbs leaf + SiteHeader wiring | PR 3a (base: PR 2 branch) | `pnpm test src/components/dashboard/__tests__/dashboard-breadcrumbs.test.tsx` | `pnpm dev` → navigate `/dashboard`, `/dashboard/bookings`, `/dashboard/bookings/abc-123`, `/dashboard/calendar`, unknown segment | `src/components/dashboard/dashboard-breadcrumbs.tsx` + `src/components/dashboard/site-header.tsx` + breadcrumb tests only |
| 3b | Slice 3b — wizard step-4 native radios + Skeleton loading + M4 regression pin | PR 3b (base: PR 3a branch) | `pnpm test src/components/bookings/__tests__/wizard-step-customer.test.tsx src/components/bookings/__tests__/wizard-progress.test.tsx` | `pnpm dev` → wizard step 4: arrow-key through mode switch, throttle patient search to see Skeleton rows | `src/components/bookings/wizard/wizard-step-customer.tsx` + wizard tests only |

Chain-strategy note: orchestrator must confirm `stacked-to-main` vs `feature-branch-chain` vs `size:exception` before apply (delivery strategy `ask-on-risk` → Decision needed: Yes). For `feature-branch-chain`: PR 1 base = feature tracker branch; PR 1b base = PR 1 branch; PR 2 base = PR 1b branch; PR 3a base = PR 2 branch; PR 3b base = PR 3a branch. If a child PR diff shows parent changes, retarget/rebase before review.

## Phase 1: Slice 1a — Auth Foundation + Login (AuthField primitive, login a11y)

- [x] 1.1 RED — Write failing RTL tests in `src/components/auth/__tests__/auth-field.test.tsx` for `AuthField`: renders label htmlFor→input id; hint `<p id={hintId}>` shown when no error; single error `<p id={errorId} role="alert">` when `error` set; hint hidden when error present. Verify: `pnpm test src/components/auth/__tests__/auth-field.test.tsx` fails (no implementation yet).
- [x] 1.2 GREEN — Create `src/components/auth/auth-field.tsx` exporting `AuthField` with props `{ id, label, required?, error?, errorId?, hint?, hintId?, children }` rendering label → children → hint → error per design contract; keep visual classes (`space-y-1.5`, `text-xs text-destructive` error). Verify: task 1.1 tests pass.
- [x] 1.3 RED — Write failing RTL tests in `src/components/auth/__tests__/login-form.test.tsx`: exactly one `h1` containing "Bienvenido de nuevo" with wordmark not a heading; credential failure (mock `signIn.email` rejection) renders single `#login-form-error[role=alert]`, both inputs `aria-invalid="true"` + `aria-describedby="login-form-error"`, `queryAllByRole("alert")` length 1, focus on alert; `noValidate` present; toggle `aria-pressed` false→true, label Mostrar→Ocultar, type password→text, Enter/Space activation; inputs carry `text-base md:text-sm`. Verify: tests fail on current `src/components/auth/login-form.tsx`.
- [x] 1.4 GREEN — Modify `src/components/auth/login-form.tsx`: single `<h1>`, wordmark to non-heading element, form-level alert (`id="login-form-error"`, `role="alert"`, `tabIndex={-1}`) with focus-on-fail, `AuthField` x2 (email, password) with explicit `aria-invalid` + `aria-describedby`, no per-field error nodes, password toggle `<button type="button" aria-pressed aria-label="Mostrar contraseña"|"Ocultar contraseña">` with Eye/EyeOff `aria-hidden` and fixed-width icon slot, inputs `h-11 text-base md:text-sm`, template-literal conditionals → `cn()`, `noValidate` kept. Verify: task 1.3 tests pass; `pnpm lint` + `pnpm type-check` clean on touched files.
- [x] 1.5 REFACTOR — Deduplicate class strings between `src/components/auth/auth-field.tsx` and `src/components/auth/login-form.tsx` (shared sizing doc comment on primitive), remove dead conditional-class code. Verify: task 1.3–1.4 tests still green.

## Phase 2: Slice 1b — Register Form (split point if slice 1 exceeds budget)

- [x] 2.1 RED — Write failing RTL tests in `src/components/auth/__tests__/register-form.test.tsx`: exactly one `h1` containing "Creá tu cuenta" with wordmark not a heading; short-password submit → password input `aria-invalid="true"` + `aria-describedby` pointing at stable `{field}-error` id, error node `role="alert"`, no duplicate error nodes per field, focus on first invalid field; per-field name/email/password errors each own one node; `noValidate` present; toggle contract (aria-pressed, Spanish labels, type swap, keyboard); `text-base md:text-sm` sizing. Verify: tests fail on current `src/components/auth/register-form.tsx`.
- [x] 2.2 GREEN — Modify `src/components/auth/register-form.tsx`: single `<h1>`, wordmark to non-heading, per-field errors (`id="{field}-error"`, `role="alert"`, input `aria-describedby` → error id plus hint id for password strength hint), focus first invalid input on failed submit, `AuthField` x3, password toggle per contract, `h-11 text-base md:text-sm`, `cn()`, `noValidate` kept. Verify: task 2.1 tests pass; `pnpm lint` + `pnpm type-check` clean.
- [x] 2.3 Verify slice 1 integration — Run full auth suite `pnpm test src/components/auth` plus `pnpm lint` + `pnpm type-check`; confirm login and register share one error idiom and no cross-form regressions. PR 1b boundary: only `src/components/auth/register-form.tsx` + `src/components/auth/__tests__/register-form.test.tsx` in this PR.

## Phase 3: Slice 2 — Feedback and Theming (tokens, Toaster, calendar, badges, WhatsApp)

- [x] 3.1 GREEN — Author missing `--status-*` tokens in `src/app/globals.css` (`:root` + `.dark` + `@theme inline` `--color-status-*` entries): `--status-awaiting-payment` orange (`oklch(0.70 0.15 75)` / dark `oklch(0.75 0.13 75)`), `--status-in-process` blue (`oklch(0.62 0.15 250)` / dark `oklch(0.72 0.13 250)`), `--status-rejected` red aligned with `--destructive` (`oklch(0.5858 0.222 17.5846)` / dark `oklch(0.6368 0.2078 25.3313)`), `--status-rescheduled` violet (`oklch(0.65 0.18 290)` / dark `oklch(0.72 0.15 290)`). Verify: tokens resolve in both themes (manual `pnpm dev` light/dark check scheduled in 3.10).
- [x] 3.2 GREEN — Author WhatsApp brand token pair in `src/app/globals.css` (`:root` + `.dark` + `@theme inline` `--color-whatsapp`, `--color-whatsapp-hover`): light `oklch(0.62 0.17 150)` / hover `oklch(0.55 0.17 150)`; dark `oklch(0.70 0.16 150)` / hover `oklch(0.76 0.14 150)`. Verify: `bg-whatsapp` / `bg-whatsapp-hover` classes compile under Tailwind v4 theme.
- [x] 3.3 RED — Write failing test asserting `src/app/layout.tsx` Toaster style uses `var(--card)` / `var(--foreground)` / `1px solid var(--border)` and that zero `hsl(var(--` remains in `src/` (source-grep test or style-prop assertion). Verify: test fails on current `hsl(var(--*))` Toaster style.
- [x] 3.4 GREEN — Modify `src/app/layout.tsx` Toaster style to `background: "var(--card)"`, `color: "var(--foreground)"`, `border: "1px solid var(--border)"`. Verify: task 3.3 test passes.
- [x] 3.5 RED (lockstep) — Update assertions in `src/components/ui/__tests__/calendar.test.tsx` FIRST to expect `var(--primary)` / `var(--primary-foreground)` (not `hsl(var(--primary))`). Verify: tests fail against current `src/components/ui/calendar.tsx`.
- [x] 3.6 GREEN — Modify `src/components/ui/calendar.tsx`: `--rdp-accent-color` → `var(--primary)`, range start/end backgrounds → `var(--primary)`, range start/end colors → `var(--primary-foreground)`, today → `var(--primary)`, range-middle `--rdp-accent-background-color` → `color-mix(in oklch, var(--primary) 15%, transparent)`. Verify: task 3.5 tests pass.
- [x] 3.7 RED (lockstep) — Update `src/modules/bookings/presentation/__tests__/status-tones.test.ts` FIRST to pin token classes (`bg-status-pending/15`, `text-status-pending-foreground`, etc.) and D1 triples (CANCELLED gray hexes, COMPLETED dark-neutral hexes), removing `amber/emerald/violet/orange-500` substring pins. Verify: tests fail against current `src/modules/bookings/presentation/status-tones.ts`.
- [x] 3.8 GREEN — Modify `src/modules/bookings/presentation/status-tones.ts`: `STATUS_TONE_CLASS` → semantic token theme classes per swap table (PENDING→`--status-pending`, CONFIRMED→`--status-confirmed`, AWAITING_PAYMENT→`--status-awaiting-payment`, NO_SHOW→`--status-no-show`, CANCELLED→`--status-cancelled` D1 gray, COMPLETED→`--status-completed` D1 dark, RESCHEDULED→`--status-rescheduled`); `STATUS_HEX` CANCELLED triple → gray hexes, COMPLETED triple → dark-neutral hexes. Then modify `src/components/dashboard/today-bookings.tsx` (`STATUS_TONE` → token classes), `src/components/bookings/booking-status-badge.tsx` (variant/tone via token classes), `src/components/payments/payment-status-badge.tsx` (PENDING→`--status-pending`, APPROVED→`--success`, REJECTED→`--destructive` variant only, CANCELLED→`--status-cancelled`, IN_PROCESS→`--info`), `src/components/dashboard/recent-activity.tsx` (`TONE` → `--info`/`--success`/`--status-completed` tints). Verify: task 3.7 tests pass; badge RTL assertions (token classes present, no raw palettes like `bg-amber-500`/`bg-red-500`/`bg-emerald-500`/`bg-yellow-500`/`bg-green-500`/`bg-sky-500`) green.
- [x] 3.9 GREEN — Modify `src/app/_landing/floating-whatsapp.tsx`: background → `bg-whatsapp`, hover → `hover:bg-whatsapp-hover`, focus ring → `focus-visible:ring-whatsapp/40`, mount-ping span → `bg-whatsapp`; keep `focus-visible:ring-4` + `aria-label`. Add/extend RTL test: className references brand token, no `green-500`/`green-600`, focus-ring present. Verify: `pnpm test` on landing/WhatsApp tests green.
- [x] 3.10 Verify slice 2 — Run `pnpm test src/components/ui src/modules/bookings/presentation src/components/dashboard src/components/payments`, `pnpm lint`, `pnpm type-check`; manual visual check in `pnpm dev` light AND dark (toasts, badges, calendar range, WhatsApp CTA); grep `hsl(var(--` in `src/` returns zero matches. No reduced-motion change (M5 deferred — do not touch).

## Phase 4: Slice 3a — Navigation Breadcrumbs

- [x] 4.1 RED — Write failing RTL tests in `src/components/dashboard/__tests__/dashboard-breadcrumbs.test.tsx` (mock `usePathname` from `next/navigation`): `/dashboard` → "Dashboard" current non-linked; `/dashboard/bookings` → "Dashboard" link → "Turnos" current; `/dashboard/bookings/abc-123` → three-level trail with "Detalle" non-linked leaf; `/dashboard/calendar` → "Calendario" current; unknown segment → graceful fallback (raw text, trail unbroken) with `aria-current="page"` on leaf. Verify: tests fail (component does not exist).
- [x] 4.2 GREEN — Create `src/components/dashboard/dashboard-breadcrumbs.tsx` (`"use client"`, `usePathname()` + es-AR label map: dashboard→Dashboard, bookings→Turnos, calendar→Calendario, patients→Pacientes, payments→Pagos, professionals→Profesionales, services→Servicios, settings→Configuración; dynamic `[id]` → "Detalle" plain text; unmapped → raw segment text). Verify: task 4.1 tests pass.
- [x] 4.3 GREEN — Modify `src/components/dashboard/site-header.tsx`: render `<DashboardBreadcrumbs />` when no explicit `crumbs` prop passed; preserve existing `crumbs`-prop path. Add RTL test: default render shows route-derived trail; explicit `crumbs` still renders as before; `SiteHeader` remains a server component (client leaf island only). Verify: `pnpm test src/components/dashboard` green. `src/app/(dashboard)/layout.tsx` untouched (still bare `<SiteHeader />` — verified, no edit).

## Phase 5: Slice 3b — Wizard Semantics + Loading

- [ ] 5.1 RED — Write failing RTL tests in `src/components/bookings/__tests__/wizard-step-customer.test.tsx`: container `getByRole("radiogroup")` with Spanish accessible label ("Modo de paciente" via fieldset/legend or aria-label); `getAllByRole("radio")` with `aria-checked` sync; `fireEvent.keyDown ArrowRight` moves focus + updates selection; assert zero `tablist`/`tab` roles on the switch. Verify: tests fail on current fake-tablist `src/components/bookings/wizard/wizard-step-customer.tsx`.
- [ ] 5.2 GREEN — Modify `src/components/bookings/wizard/wizard-step-customer.tsx` (mode switch only in this task): replace fake `tablist`/`tab` buttons with native `<input type="radio" name="wizard-customer-mode">` options in `<fieldset>` + `<legend>Modo de paciente</legend>`, styled as segmented cards via `peer-checked:` + `cn()`; `mode`/`onModeChange` props unchanged; no new dependencies. Verify: task 5.1 tests pass.
- [ ] 5.3 RED — Write failing RTL tests for patient-search loading in `src/components/bookings/__tests__/wizard-step-customer.test.tsx` (or adjacent loading test): loading state renders three `<Skeleton className="h-[68px] w-full rounded-lg" />` with `role="status"` + `aria-label="Buscando pacientes…"`, and no `Loader2`/spinner present. Dashboard loading fallbacks: `Skeleton` rows matching layouts (table `h-10`, activity `h-12`). Verify: tests fail on current spinner markup.
- [ ] 5.4 GREEN — Modify `src/components/bookings/wizard/wizard-step-customer.tsx` (loading part) + touched dashboard loading fallbacks: replace `Loader2` spinner row with dimension-matched `Skeleton` rows; button-level submit spinners stay (explicitly permitted). Verify: task 5.3 tests pass.
- [ ] 5.5 M4 regression pin — Confirm `src/components/bookings/wizard/wizard-progress.tsx` untouched (already `aria-current="step"` + number/label text); keep existing `wizard-progress.test.tsx` green as the bookings-spec "Progress indicator non-color channel" coverage. If gaps found, add assertions (current step `aria-current="step"` + visible text label). Verify: `pnpm test src/components/bookings/__tests__/wizard-progress.test.tsx` green.

## Phase 6: Cross-Slice Verification and Cleanup

- [ ] 6.1 Full suite + guards — Run `pnpm test`, `pnpm lint`, `pnpm type-check` green across all slices; grep `hsl(var(--` in `src/` → zero matches; grep raw-palette classes in touched status surfaces (`bg-red-500`, `bg-sky-500`, `bg-emerald-500`, `bg-amber-500`, `bg-yellow-500`, `bg-green-500`, `green-600`) → zero matches in status coloring. Verify: all green, no violations.
- [ ] 6.2 Spec-coverage audit — Walk each of the 8 delta/new specs (`auth-module`, `ui-feedback`, `dashboard-shell`, `bookings`, `calendar-view`, `calendar-ui`, `payments-presentation`, `landing-public`) scenario-by-scenario against implemented tests; file any gap as a new RED task before closing the slice. Verify: every scenario has at least one proving test.
- [ ] 6.3 Rollback hygiene — Confirm per-slice revert boundaries hold (slice 1a / 1b / 2 / 3a / 3b independently revertible per work-unit table); restore point `backup/pre-ux-audit-fixes` @ `3a71fc4` still valid. No backend/domain/data/actions/DB/schema files touched; no deletions.
