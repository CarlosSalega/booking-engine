/**
 * Professionals module — presentation barrel.
 *
 * Re-exports the es-AR formatters (status labels, specialties formatting).
 * Consumers should import from `@/modules/professionals/presentation`
 * (or via the module barrel) rather than reaching into individual files.
 */

export * from "./formatters";
